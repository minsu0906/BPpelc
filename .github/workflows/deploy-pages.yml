name: Pages Maintenance

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deployment mode
        run: echo "GitHub Pages is currently configured to deploy from the main branch root."
